server:
  port: 33001

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10002/eureka/
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"